# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# The backend/ FastAPI server (see ../backend/README.md) -- must be running
# for trip planning, stop resolution, and the nearest-stop picker to work.
VITE_BACKEND_URL=http://localhost:8000
